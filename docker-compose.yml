services:
  dashboard:
    build:
      context: .
      dockerfile: apps/web/Dockerfile
    ports:
      - "3000:3000"
    volumes:
      - ~/.claude:/home/node/.claude:ro
    environment:
      - NODE_ENV=production
